apiVersion: v1
kind: Service
metadata:
  name: {{ include "polling-chart.fullname" . }}
  labels:
    {{- include "polling-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "polling-chart.selectorLabels" . | nindent 4 }}
